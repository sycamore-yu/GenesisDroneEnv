# APG 和 SHAC 分别确定默认并行环境数

在当前 8 GB GPU 上，APG 和 SHAC 分别按 2 的幂次测试并行环境数。每次测试必须完成 32 步 Genesis 前向、演员反向和对应算法更新；选择仍保留约 20% 显存余量的最大稳定值。默认值写入各自配置，不在运行时自动探测，并允许命令行覆盖。最终报告各算法实际环境数、环境步数、峰值显存和墙钟训练时间，不把不同环境数包装成等预算比较。
