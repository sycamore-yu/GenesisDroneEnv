# diffRL 复用现有 PPO 随机任务分布

APG/SHAC 训练直接复用现有 PPO 范围，不增加课程学习：初始 `x,y` 均为 `[-0.05, 0.05] m`，初始 `z` 为 `[0.60, 0.61] m`，初始偏航在 `[-π, π]` 均匀分布且滚转、俯仰为零；每个航点的 `x,y` 均为 `[-1.2, 1.2] m`，`z` 为 `[0.6, 1.0] m`。验证集和测试集从同一分布预生成，但场景互不重叠。
