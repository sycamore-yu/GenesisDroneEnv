# 可微窗口内屏蔽失败环境

一个完整训练回合由多个短可微窗口组成。窗口内禁止局部 `Scene.reset()`：使用 `alive_before` 保留坠毁步骤的运行损失和终止损失，随后将该环境的动作、损失和价值估计归零；窗口末尾反向传播并清理梯度时间轴，同时保留物理状态。只在完整训练回合边界统一重置和重新随机化全部环境。该设计服从 Genesis 全批量共享可微时间轴的约束，并避免未经验证的局部状态传送进入训练主路径。
