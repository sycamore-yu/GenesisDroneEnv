# 04 — 完整 APG（解析策略梯度）固定七门训练

**What to build（实施目标）：** `apg（解析策略梯度）` 成为完整时间反向传播实现。正式训练不再切断观测梯度。连续竞速损失能通过共享控制器和 Genesis（仿真器）动力学更新演员。

**Blocked by（前置工单）：** 03 — 固定七门 PPO（近端策略优化）完整基线。

**Status（状态）：** ready-for-agent（可交给智能体）

- [x] 删除解耦状态 APG（解析策略梯度）的正式训练路径和正式名称。
- [x] 旧检查点只带 `legacy（旧版本）` 信息，不提供旧算法分支。
- [x] 连续损失包含进度、加速度、加速度变化率、门向量场和门框安全。
- [x] 门框安全损失的负位置梯度远离门框。
- [x] 32、64、96 步窗口可由配置选择，控制周期保持 0.01 秒。
- [x] 小规模优化检查显示参数梯度有限且目标指标改善。
