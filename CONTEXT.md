# 无人机学习任务

本上下文统一无人机学习任务中的业务术语，避免把不同目标行为混为一谈。

## Language

**随机航点到达任务**：
无人机依次飞向随机生成的静止航点；到达当前航点后，任务生成下一个航点。
_Avoid_: 点追踪任务、移动目标追踪

**航点**：
无人机当前需要到达的静止三维位置。
_Avoid_: 追踪点、移动目标

**到达**：
无人机与当前航点的距离小于 0.1 米；速度和姿态不影响该判定。
_Avoid_: 稳定悬停

**坠毁**：
无人机机体中心高度低于 0.1 米、相对航点的横向误差超过 5 米、竖向误差超过 1.2 米，或状态出现非有限数值；姿态角本身不触发坠毁。
_Avoid_: 大倾角

**四旋翼控制作用量**：
策略给出的四维控制量。每个任务必须明确它的物理含义，不能默认把不同控制接口混在一起。
_Avoid_: 六维任意力、所有任务共用未说明的动作含义

## 竞速

**竞速任务**：
无人机按正确方向和顺序穿过已知赛道地图中的门。第一阶段只包含门框，不包含独立障碍物。
_Avoid_: 门识别任务、动态避障任务

**CTBR（总推力与机体角速度指令）控制作用量**：
四维竞速控制作用量，由质量归一化总推力和三个期望机体系角速度组成。
_Avoid_: 转动力矩动作、角度指令

**赛道描述**：
任意数量门的门位置、姿态、尺寸和通行顺序。固定七门只是第一份赛道描述。
_Avoid_: 写死七门坐标的赛道逻辑

**已知赛道地图**：
真实或仿真中预先给出的门位置和姿态；状态估计器用它计算门相对无人机的几何关系。
_Avoid_: 通过激光雷达识别门

**安全门洞**：
门洞在无人机保守球形安全半径后剩余的可通过区域。
_Avoid_: 只按门中心点判断穿门

## 感知与避障

**动态避障任务**：
无人机只根据自身状态估计、任务指令和传感器观测避开静态或运动障碍物的任务。
_Avoid_: 已知赛道地图竞速任务

**Mid-360（激光雷达型号）采样模型**：
按真实扫描方向、顺序和时间生成原始激光雷达点的传感器物理模型。
_Avoid_: 策略网络输入编码

**感知编码**：
把原始点云变成策略可读取的固定形状特征的规则，例如 3200 维最近距离向量或距离加点流图。
_Avoid_: 激光雷达采样模型

**演员观测**：
真实部署时策略可以读取的输入，例如传感器特征、状态估计和任务指令。
_Avoid_: 仿真真值障碍状态

**特权评论家观测**：
只在训练时给评论家的仿真真值输入，例如障碍位置、速度和剩余任务信息。
_Avoid_: 真实部署策略输入

**完整 APG（解析策略梯度）**：
从状态到策略、动作、动力学和下一状态保持完整反向传播的状态任务训练方法。
_Avoid_: 解耦传感器策略梯度

**D.VA（解耦视觉解析策略梯度）**：
策略把传感器观测当作常量输入，但仍通过可微动力学和终端价值更新策略的训练方法。
_Avoid_: 完整 APG（解析策略梯度）

**可微训练损失**：
用于更新策略的连续目标值，能沿控制作用量和仿真状态反向传播。
_Avoid_: 评测奖励

**连续日志奖励**：
由常数 1 减去独立加权的连续惩罚得到，并立即切断梯度；用于训练日志，不用于 APG 或 SHAC 更新。
_Avoid_: 负训练损失

**可微安全距离**：
无人机最低碰撞几何点到地面的净空，以及当前位置到任务状态边界的剩余距离；通过平滑势函数在坠毁前提供梯度。
_Avoid_: 碰撞布尔事件

**任务评测指标**：
用于比较策略表现的结果，包括首次到达比例、每回合和每分钟航点数、平均到达时间、坠毁率、存活时间和位置误差；它不参与梯度计算。
_Avoid_: 可微训练损失

**可微窗口**：
连续执行并记录的固定步数仿真片段；窗口末尾反向传播并切断梯度，但保留物理状态。
_Avoid_: 完整训练回合

**完整训练回合**：
从全环境随机初始化开始，到 1500 个仿真步完成或全部环境失效为止；它可以包含多个可微窗口。
_Avoid_: 可微窗口

**存活环境**：
尚未坠毁并继续贡献动作、可微训练损失和未来价值估计的并行环境。
_Avoid_: 已终止环境

**偏航局部坐标系**：
只跟随机体偏航旋转、但始终保持竖直方向固定的坐标系。
_Avoid_: 完整机体系、世界坐标系
